name: RegionInformationReload
version: '${project.version}'
main: com.github.pregrafer.RegionInformationReload.RegionInformationReload
prefix: RegionInfoRe
api-version: 1.13
authors: [ pop2wo ]
softdepend: [ PlaceholderAPI ]
commands:
  regioninformationreload:
    description: Main Command
    aliases:
      - rir
  itemtype:
    description: Get item type In Hand
    aliases:
      - it
